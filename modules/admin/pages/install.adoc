:sserv-inst:

= Установка Сервиса настроек

include::6.2@mgmtconsole:servers:partial$excerpts.adoc[tags=must-admin]

include::6.1@workerservice::partial$excerpts.adoc[tags=before-install]

[#linux-server]
== Установка {of-sett-serv} на сервер под управлением Linux

// tag::linux[]
.Чтобы установить {sett-serv} на сервер под управлением Linux:
:of: {of-sett-serv}
:file: appsettings
:wt: settingsservice
:no-config: yes
include::6.1@platform:admin:partial$install.adoc[]
// end::linux[]

// tag::windows[]
[#windows-server]
== Установка {of-sett-serv} на сервер под управлением Windows

.Чтобы установить {sett-serv} на сервер под управлением Windows:
. Откройте пакет установки `{dv} Settings service.msi`.
+
.Установка {of-sett-serv}
image::install-server-hello.png[Установка {of-sett-serv}]
+
. Нажмите *Далее*, ознакомьтесь с условиями лицензионного соглашения. Подтвердите, что вы согласны с лицензионным соглашением и нажмите *Далее*.
+
.Лицензионное соглашение
image::install-server-license.png[Лицензионное соглашение]
+
[#components]
. Выберите компоненты, которые требуется установить, и нажмите *Далее*.
+
.Выбор устанавливаемых компонентов
image::install-server-components.png[Выбор устанавливаемых компонентов]
+
include::6.1@platform:admin:partial$install-components.adoc[]
+
****
// tag::components[]
Общие компоненты::
Обеспечивают взаимодействие с сервером {dv}, обязательны для установки на сервер {dv}.

Служба Windows:::
Основные компоненты {of-sett-serv}, предоставляющие базовую функциональность.
// end::components[]

Кнопка Сброс::
Сбрасывает выбор компонентов на стандартные.

Кнопка Использование диска::
Позволяет посмотреть свободное место на системных дисках.
****
+
. Нажмите на кнопку *Установить* и дождитесь завершения установки модуля.
+
.Мастер установки серверной части модуля {bo}
image::install-server-confirm.png[Мастер установки серверной части модуля {bo}]
+
. На следующем шаге нажмите кнопку *Готово*, чтобы закрыть мастер установки.

// end::windows[]

[#config-file]
== Настройка {of-sett-serv}

// tag::config[]
. {blank}
+
[subs="specialchars,attributes,quotes"]
В конфигурационном файле по адресу {config-ss} необходимо выполнить минимальные настройки: `ApiKey`, `StorageType`, `StorageOptions`, `EncryptionCertificate`.
+
--
.Фрагмент конфигурационного файла:
[source,json]
----
{
  "ApiKey": "SettingsServiceApiKey", <.>
  "StorageOptions": {
    "Providers": [
      {
        "Alias": "SqlStorage",
        "Default": true,
        "StorageType": "DB-type", <.>
        "StorageOptions": "CONNECTION-STRING", <.>
        "ProviderType": "Hierarchy"
      }
    ]
  }
}
----
<.> Ключ доступа к Сервису настроек -- "пароль", который также необходимо указать в конфигурационном файле модулей {mc}, {pl}, {wc}, {wf}, Служба {ws} и {fs}.
<.> Тип БД: `MsSql` или `PgSql`.
<.> Строка подключения к БД. См. подробнее "<<conn-string,Строка подключения к базе данных для хранения настроек>>" далее.
ifndef::installation[]
При наличии БД {of-sett-serv} предыдущей версии, можно использовать её.
endif::[]
--
+
. После сохранения настроек в конфигурационном файле запустите службу {of-sett-serv}.
ifndef::windows[]
+
:no-root-since: ss-no-root-since
include::6.1@webclient:admin:partial$excerpts.adoc[tags=no-root]
endif::[]
ifndef::linux[]
include::6.1@webclient:admin:partial$excerpts.adoc[tags=nt-service]
endif::[]
// <.> Настройки, записываемые в {sett-serv}, рекомендуется защищать при помощи шифрования. Для этой цели используется самоподписанный сертификат, поддерживающий шифрование/дешифрование данных. Создайте сертификат локально, например, по инструкции в разделе "xref:dv6@engineer:ROOT:appendix/account-protection.adoc#cr-cert[Защита системной учётной записи]", и установите его в хранилище сертификатов "Доверенные корневые центры сертификации" на уровне "Локальный компьютер": на всех серверах, данные которых шифруются, сервере {of-mc} и серверах Сервиса настроек.
// <.> Путь к публичной части сертификата для шифрования параметров в Сервисе настроек.
// end::config[]

// tag::cluster[]
[#cluster]
== Настройка кластера {of-sett-serv}

Для обеспечения надежности рекомендуется использовать кластер {of-sett-serv} и регулярно производить резервное копирование базы данных Сервиса.

.Чтобы настроить кластер {of-sett-serv}, выполните следующие действия:
. Установите {sett-serv} на первом и последующих узлах кластера согласно
ifndef::sserv-inst[]
инструкции
endif::sserv-inst[]
ifdef::sserv-inst[]
<<linux-server,для Linux>> или <<windows-server,для Windows>>.
endif::sserv-inst[]
. Убедитесь, что на всех узлах используется один и тот же API ключ доступа и одну и ту же БД для хранения настроек.
. Настройте балансировщик нагрузкиfootnote:[Описание настройки балансировщика выходит за рамки документации {dv}.] с режимом active-passive и укажите адрес балансировщика в конфигурационных файлах компонентов, зарегистрированных в текущем Сервисе настроек.
// end::cluster[]

// tag::string[]
:of-what: {of-sett-serv}

[#conn-string]
== Строка подключения к базе данных для хранения настроек

Строка подключения содержит адрес подключения к базе данных для хранения настроек. Вид строки подключения зависит от выбранной базы данных.

WARNING: Для {of-what} необходимо использовать отдельную базу данных. Если БД {of-what} уже создавалась, можно использовать её.

//tag::mssql[]
ifndef::installation,upgrade[]
Для {mssql} Server: `Data Source=Адрес-сервера-баз-данных;Initial Catalog=Название-БД-{of-what};Integrated Security=False;User ID=Имя-пользователя-БД;Password=Пароль-пользователя-БД;TrustServerCertificate=false`.
endif::[]
ifdef::installation,upgrade[]
Для {mssql} Server: `Data Source=Адрес-сервера-баз-данных;Initial Catalog=Название-БД-{of-what};Integrated Security=False;User ID=Имя-пользователя-БД;Password=Пароль-пользователя-БД`.
endif::[]

* `Data Source` -- адрес сервера SQL для подключения к БД. В качестве `Data Source` может быть указан экземпляр SQL-сервера, например так: `Data Source=DVDatabse\\Server1`.
* `Название-БД-{of-what}` -- псевдоним базы данных.
* `Integrated Security` -- задает логическое значение, определяющее способ проверки подлинности:
** `False` -- при подключении в строке должны быть указаны идентификатор пользователя и пароль.
** `True` -- при подключении будут использованы учётные данные текущей учётной записи Windows.
* `Имя-пользователя-БД` -- учётная запись пользователя для подключения к БД.
* `Пароль-пользователя-БД` -- пароль учётной записи для подключения к БД.
ifndef::installation,upgrade[]
* `TrustServerCertificate=false` -- подробнее про настройку см. <<cert,особенности настройки сертификата>>.
endif::[]

NOTE: При использовании строки подключения к хранилищу с Windows-аутентификацией для {mssql} Server учётной записи службы _{of-what}_ необходимо предоставить права на создание новых БД, а также полные права на БД, которая будет создана при запуске _{of-what}_.

ifndef::sm-page[]
Если в строке подключения введены корректные данные, при запуске _{of-what}_ будет создана новая или обновлена существующая БД.
endif::sm-page[]
//end::mssql[]

//tag::pgsql[]
Для {pgsql}/{pgl}: `host=Адрес-сервера-баз данных;Port=SQL-порт;database=Название-БД-{of-what};Username=Имя-пользователя-БД;Password=Пароль-пользователя-БД`.

* `Адрес-сервера-баз данных` -- адрес сервера SQL для подключения к БД.
* `SQL-порт` -- порт SQL-сервера.
* `Название-БД-{of-what}` -- псевдоним базы данных.
* `Имя-пользователя-БД` -- учётная запись пользователя для подключения к БД.
* `Пароль-пользователя-БД` -- пароль учётной записи для подключения к БД.
//end::pgsql[]
// end::string[]

// tag::cert[]
[#cert]
== Особенности настройки сертификата

Настройка актуальна только для {mssql} Server, для {pgsql} дополнительных настроек не требуется. По умолчанию провайдер считает соединение с БД защищённым.

Если соединение не защищено (т.е. не настроен сертификат на уровне БД, используется самозаверенный или недействительный сертификат), необходимо в строке соединения указывать `TrustServerCertificate = true`, это сообщит провайдеру, что серверу можно доверять.

Настоятельно рекомендуется использовать защищённое соединение, настроив {mssql} сервер в соответствии с инструкцией в руководстве по настройке системы, см. подробнее "xref:dv6@engineer:ROOT:create-cert.adoc[]".
// end::cert[]

[#deletion]
== Удаление {of-sett-serv}

* Чтобы удалить модуль в ОС Windows, воспользуйтесь стандартными средствами системы (Установка и удаление программ).
* Чтобы удалить модуль в ОС Linux, выполните команду:
+
include::6.1@platform:admin:partial$delete.adoc[]

Созданные группы и службы будут удалены, _{sett-serv}_ станет недоступен.
